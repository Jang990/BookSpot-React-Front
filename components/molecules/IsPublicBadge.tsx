import { GrayBadge, GreenBadge } from "../atoms/badge/TextLabelBadge";

export const IsPublicBadge = ({ isPublic }: { isPublic: boolean }) => {
  return (
    <>
      {/* {isPublic ? <GreenBadge text="공개" /> : <GrayBadge text="비공개" />} */}
    </>
  );
};
